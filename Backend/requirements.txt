# Repo path: Backend/requirements.txt  (UPDATED)
fastapi==0.115.0
uvicorn[standard]==0.30.6
sqlalchemy==2.0.35
pydantic==2.9.2
pydantic-settings==2.5.2
python-multipart==0.0.9
Pillow==10.4.0
psycopg2-binary==2.9.9

# python-dotenv is required by pydantic-settings to read Backend/.env.
# Without it, app/core/config.py raises ModuleNotFoundError at import
# time and the server fails to start (hit live on Camber deployment).
python-dotenv==1.0.1

httpx==0.27.2

# MobileSAM dependencies
torch==2.4.1
torchvision==0.19.1
numpy==1.26.4
git+https://github.com/ChaoningZhang/MobileSAM.git@master#egg=mobile_sam

pytest==8.3.3

# Generation model (SDXL Inpainting, base model for now — LoRA added later)
# NOTE: diffusers/transformers/accelerate are PINNED, not >=, on purpose.
# Newer diffusers (0.31+) added a flash-attention-3 custom-op registration
# path (models/attention_dispatch.py) that is incompatible with torch
# 2.4.1's older custom-op type inference and crashes on import with:
#   "infer_schema(func): Parameter q has unsupported type torch.Tensor"
# This exact combination (diffusers==0.30.0 + transformers==4.44.2 +
# accelerate==0.33.0 + torch==2.4.1) is confirmed working end-to-end.
# Do not bump these without re-testing a full server startup.
diffusers==0.30.0
transformers==4.44.2
accelerate==0.33.0
peft>=0.7.0

# transformers/tokenizers transitively need these; pinning explicitly
# since they were missing at runtime on a fresh --user install and
# caused ModuleNotFoundError mid-import chain.
regex==2024.9.11
safetensors==0.4.5